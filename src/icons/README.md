Intentionally empty.

astro-icon scans this directory for local SVGs and warns at build time when it
is missing. The site uses the Phosphor set via @iconify-json/ph, and
tasks/design-direction.md bans hand-rolled SVG icons, so nothing should ever
land here.
